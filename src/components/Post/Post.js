import React from 'react';



const Post = ({getPostId}) => {
    return (
        <div>
            {/*<button onClick={()=> getPostId }>full</button>*/}
        </div>
    );
};

export default Post;